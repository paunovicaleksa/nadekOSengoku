#ifndef NADEKOSENGOKUPRIVATE_MISC_H
#define NADEKOSENGOKUPRIVATE_MISC_H
#include "../lib/hw.h"

int log2_64(size_t sz);
size_t ceil_2(size_t sz);

void memset(const void* data, char val, int size);

#endif //NADEKOSENGOKUPRIVATE_MISC_H
